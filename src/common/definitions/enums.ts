export enum EAdditional {
  MatteFinish = 'Matte Finish',
  Numbering = 'Numbering',
  ShrinkWrapping = 'Shrink Wrapping',
}

export enum ECreateVinylSteps {
  CustomizeJacket = 0,
  CustomizeVinyl = 1,
  VinylInfo = 2,
  Tracks = 3,
  Quantity = 4,
  ArtistInfo = 5,
  Publish = 6,
  Submit = 7,
}

export enum EJacket {
  Single = 'Single',
  Gatefold = 'Gatefold',
}

export enum ESpine {
  DefaultColor = 'Default Color',
  ImageFoldOver = 'Image Fold Over',
}

export enum EVinylWeights {
  Weight140gr = '140 gr',
  Weight180gr = '180 gr',
}

export enum ELabelTypes {
  SimpleLabel = 'Simple Label',
  UploadLabel = 'Upload a Label',
}

export enum ELaunchTypes {
  Quantity = 'Quantity',
  Presale = 'Presale',
}
